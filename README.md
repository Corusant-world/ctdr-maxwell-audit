# Maxwell's Demon — David vs Goliath (public-safe)

This repo is a public-safe evidence bundle + dashboard for comparing H100 long-context compute regimes.

## Open the dashboard

- `maxwell_dashboard/index.html` (narrative + clickable evidence)
- `maxwell_dashboard/compare.html` (compare two packs by uploading `summary_public.json`)

## Pack Standard v1 (community submissions)

- `pack_format/summary_public.schema.json`
- `pack_tools/validate_summary_public.py`

## Evidence bundle

- `evidence_public/evidence.zip` (contains the dashboard + receipts + schemas)

Hard rule: no kernel source / PTX / SASS / bindings are included.

